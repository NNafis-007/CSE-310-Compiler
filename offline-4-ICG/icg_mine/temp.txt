main PROC
	MOV AX, @DATA
	MOV DS, AX
	PUSH BP
	MOV BP, SP

	MOV AX, 1
	MOV i, AX
	MOV AX, i		; Line 4
	CALL print_output
	CALL new_line

	MOV AX, 8
	MOV DX, AX
	MOV AX, 5
	ADD AX, DX
	PUSH AX
	POP AX
	MOV j, AX
	MOV AX, j		; Line 7
	CALL print_output
	CALL new_line


	POP BP
	MOV AX, 4Ch
	INT 21h
main ENDP
